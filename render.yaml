services:
  - type: web
    name: hostelassist
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
    startCommand: gunicorn Hostelassist.wsgi:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0


